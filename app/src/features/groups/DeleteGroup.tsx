import SettingsRow from "./SettingsRow";

export default function DeleteGroup() {
  return (
    <SettingsRow
      icon="Delete"
      title="Delete and Leave Group"
      subtitle=""
      onClick={() => {}}
    />
  );
}
